;;;; FiveAM tests for CLI daemon commands

(in-package :cl-claw.cli.tests)

(declaim (optimize (safety 3) (debug 3)))

(in-suite cli-suite)

;; Tests for daemon CLI commands

(test daemon-cli-start
  "Daemon start command"
  ;; TODO: Implement when daemon CLI commands are available
  (skip "daemon CLI commands not yet available"))

(test daemon-cli-stop
  "Daemon stop command"
  ;; TODO: Implement when daemon CLI commands are available
  (skip "daemon CLI commands not yet available"))

(test daemon-cli-status
  "Daemon status command"
  ;; TODO: Implement when daemon CLI commands are available
  (skip "daemon CLI commands not yet available"))

(test daemon-cli-restart
  "Daemon restart command"
  ;; TODO: Implement when daemon CLI commands are available
  (skip "daemon CLI commands not yet available"))

(test daemon-cli-coverage
  "Daemon coverage command"
  ;; TODO: Implement when daemon CLI commands are available
  (skip "daemon CLI commands not yet available"))
